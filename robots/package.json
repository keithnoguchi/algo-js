{
	"name": "robots",
	"version": "1.0.0",
	"main": "src/main.js",
	"license": "MIT",
	"scripts": {
		"lint": "prettier --plugin-search-dir . --check . && eslint .",
		"format": "prettier --plugin-search-dir . --write ."
	},
	"dependencies": {
		"eslint": "^8.36.0",
		"prettier": "^2.8.7"
	}
}
